<Window x:Class="Animate1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Animate1"
        xmlns:uc="clr-namespace:Animate1.Page"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
                <RowDefinition Height ="1*"/>
                <RowDefinition Height ="9*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" >
                <Button x:Name="btnCircle" Width="100" Content="Circle" FontSize="20" Click="btnCircle_Click" BorderBrush="Black" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <!-- Call user controls PageSelect, Binding ElementName is for keep max width no matter resolutaion -->
            <uc:PageCircle x:Name="contentPage"/>
        </Grid>
    </Grid>
</Window>
